// @symbiote-native/asset/solid: the Solid entry over the framework-agnostic core — mirrors the
// lifecycle-bucket naming convention of adapters/solid/src/primitives (never `hooks`/
// `composables`/`runes`, those are React's, Vue's and Svelte's terms).

export { createAssets } from './primitives/create-assets';
export * from '../core';
