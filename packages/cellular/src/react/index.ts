// @symbiote-native/cellular/react: the React entry over the framework-agnostic core, plus
// usePermissions — the only stateful surface this package needs a lifecycle wrapper for.
export { usePermissions } from './hooks/use-permissions';
export * from '../core';
