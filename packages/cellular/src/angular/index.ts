// @symbiote-native/cellular/angular: the Angular entry over the framework-agnostic core, plus
// PermissionsService — the only stateful surface this package needs a lifecycle wrapper for.
export { PermissionsService } from './services/permissions.service';
export * from '../core';
