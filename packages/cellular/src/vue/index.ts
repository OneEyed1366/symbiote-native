// @symbiote-native/cellular/vue: the Vue entry over the framework-agnostic core, plus
// usePermissions — the only stateful surface this package needs a lifecycle wrapper for.
export { usePermissions } from './composables/use-permissions';
export * from '../core';
