// @symbiote-native/font/solid: the Solid entry over the framework-agnostic core — mirrors the
// lifecycle-bucket naming convention of adapters/solid/src/primitives (never `hooks`/
// `composables`/`runes`, those are React's, Vue's and Svelte's terms).

export { createFonts } from './primitives/create-fonts';
export * from '../core';
