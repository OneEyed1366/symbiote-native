---
title: How-tos
description: Task-oriented recipes for common SymbioteJS problems.
---

Short, direct answers to "how do I do X" — each page is a lookup, not a
tutorial. For the concept behind an answer, follow the link into
[Learn](/symbiote-native/docs/learn/react/) or [API](/symbiote-native/docs/api/); for a full copyable app, see
[Examples](/symbiote-native/docs/examples/).

## Available how-tos

- [Style a component](/symbiote-native/docs/howtos/styling/) — `StyleSheet.create` vs a Vue SFC
  `<style>` block.
- [Handle press/change events](/symbiote-native/docs/howtos/events/) — React callback props,
  Vue emits, Angular `@Output()`.
- [Two-way bind a value](/symbiote-native/docs/howtos/two-way-binding/) — Vue `v-model`,
  Angular `[(value)]`.
- [Share content across surfaces](/symbiote-native/docs/howtos/portals-and-tunnels/) —
  `createPortal`/`Teleport` vs `createTunnel`.
- [Write platform-specific code](/symbiote-native/docs/howtos/platform-code/) — the
  `X.ios.ts`/`X.android.ts`/`X/shared.ts` folder convention.
- [Wrap a third-party native view](/symbiote-native/docs/howtos/third-party-views/) — why the
  library's React component can't be reused, and what can.
- [Turn on diagnostic logging](/symbiote-native/docs/howtos/debugging/) — `dlog`/`isDebug` and
  the `DEBUG` env var.
