// Settings: base / headless build. Metro picks index.ios.ts / index.android.ts on a device; off
// those, the iOS build (the SettingsManager-backed one) is the default.

export * from './index.ios';
