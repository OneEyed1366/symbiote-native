---
'@symbiote-native/background-fetch': patch
---

Add `@symbiote-native/background-fetch`, wrapping `expo-background-fetch` — registers periodic background sync through `@symbiote-native/task-manager`'s `defineTask`.
